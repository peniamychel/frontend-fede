tags:
  integracion:
    description: >-
      Pruebas que golpean el backend real. Necesitan Spring Boot corriendo y que
      se fuerce el host, porque flutter_test finge ser Android y ApiConfig
      resolvería 10.0.2.2:
      flutter test --dart-define=API_HOST=localhost
