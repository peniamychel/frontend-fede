<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- La app consume la API del padrón por HTTP. Flutter solo declara este
         permiso en los manifiestos de debug y profile, así que sin esta línea
         el APK de release se compila bien pero se queda sin red. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Para leer el QR de las credenciales al pasar lista. La cámara no es
         obligatoria: el teléfono sin cámara igual sirve, porque el código se
         puede escribir a mano. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:label="Padrón Carrasco Tropical"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
             Clave de Google Maps para Android. Es una clave distinta de la de
             la web: se restringe por nombre de paquete y huella SHA-1, no por
             referente HTTP.

             Reemplazá TU_API_KEY_AQUI por la tuya. Sin ella el mapa no carga,
             pero la pantalla de ubicación sigue funcionando con coordenadas
             manuales.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="TU_API_KEY_AQUI"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
